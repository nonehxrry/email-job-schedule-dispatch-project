{
  "name": "reachinbox-scheduler-root",
  "version": "1.0.0",
  "description": "ReachInbox Full-Stack Email Job Scheduler with BullMQ, Redis, PostgreSQL, and React",
  "private": true,
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "build:backend": "npm run build --prefix backend",
    "build:frontend": "npm run build --prefix frontend",
    "build": "npm run build:frontend && npm run build:backend",
    "start": "cd backend && npx prisma db push && node dist/server.js",
    "prisma:generate": "npm run prisma:generate --prefix backend",
    "prisma:push": "npm run prisma:push --prefix backend",
    "test": "npx tsx backend/src/test-runner.ts"
  },
  "keywords": [
    "reachinbox",
    "bullmq",
    "redis",
    "email-scheduler",
    "outbox-labs",
    "rate-limiting",
    "slack-notifications"
  ],
  "author": "ReachInbox Candidate",
  "license": "MIT"
}
